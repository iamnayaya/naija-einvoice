strict-peer-dependencies=false
auto-install-peers=true
link-workspace-packages=true
# Prisma generate bootstraps its own `pnpm add @prisma/client` at the workspace
# root; pnpm blocks root adds by default. It's already a dependency, so the
# add is a no-op — this just lets it through.
ignore-workspace-root-check=true
